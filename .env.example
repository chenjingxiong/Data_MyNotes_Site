# OpenResty 服务器部署配置示例
# 复制此文件为 .env 并填写实际值

# ===============================
# SSH 连接配置
# ===============================
# 服务器地址
SERVER_HOST=your-server.com
# SSH 端口 (默认: 22)
SERVER_PORT=22
# SSH 用户名
SSH_USER=root
# SSH 私钥 (GitHub Actions 使用，从 GitHub Secrets 中配置)
# SSH_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----...
# 本地部署时使用默认的 SSH 密钥 (~/.ssh/id_rsa)

# ===============================
# 部署路径配置
# ===============================
# 网站部署路径
DEPLOY_PATH=/usr/local/openresty/nginx/html/mynotes
# 备份路径
BACKUP_PATH=/var/backups/mynotes
# 网站名称 (用于备份文件命名)
SITE_NAME=mynotes

# ===============================
# Nginx 配置
# ===============================
# Nginx 运行用户
NGINX_USER=nginx
# Nginx 测试配置命令
NGINX_TEST_CMD=/usr/local/openresty/nginx/sbin/nginx -t
# Nginx 重载命令
NGINX_RELOAD_CMD=/usr/local/openresty/nginx/sbin/nginx -s reload

# ===============================
# GitHub Actions Secrets 配置
# ===============================
# 在 GitHub 仓库设置中添加以下 Secrets:
#
# 1. SSH_PRIVATE_KEY - SSH 私钥内容
#    生成方法: ssh-keygen -t rsa -b 4096 -C "github-actions"
#    将私钥内容添加到 GitHub Secrets
#
# 2. SSH_PORT - SSH 端口 (默认: 22)
#
# 3. SSH_USER - SSH 用户名 (默认: root)
#
# 4. SERVER_HOST - 服务器地址
#
# 5. DEPLOY_PATH - 部署路径
#
# 6. BACKUP_PATH - 备份路径
#
# 7. SITE_NAME - 网站名称
#
# 8. NGINX_USER - Nginx 运行用户
#
# 9. NGINX_TEST_CMD - Nginx 测试配置命令
#
# 10. NGINX_RELOAD_CMD - Nginx 重载命令
