# OpenResty Nginx 配置示例
# 文件位置: /usr/local/openresty/nginx/conf/conf.d/mynotes.conf

server {
    listen 80;
    listen [::]:80;
    server_name notes.example.com;

    # 可选: 自动重定向到 HTTPS
    # return 301 https://$server_name$request_uri;

    # 网站根目录
    root /usr/local/openresty/nginx/html/mynotes;
    index index.html;

    # 日志配置
    access_log /var/log/nginx/mynotes_access.log;
    error_log /var/log/nginx/mynotes_error.log;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/rss+xml
        application/atom+xml
        image/svg+xml;

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # HTML 文件不缓存
    location ~* \.html$ {
        add_header Cache-Control "no-cache, must-revalidate";
    }

    # VitePress SPA 路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
}

# HTTPS 配置 (可选)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name notes.example.com;
#
#     # SSL 证书配置
#     ssl_certificate /path/to/your/certificate.crt;
#     ssl_certificate_key /path/to/your/private.key;
#
#     # SSL 配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # HSTS (可选)
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     # 其他配置与 HTTP 相同
#     root /usr/local/openresty/nginx/html/mynotes;
#     index index.html;
#
#     # ... (其他配置项同上)
# }
